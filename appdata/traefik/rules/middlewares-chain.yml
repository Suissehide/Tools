http:
  middlewares:
    chain-no-auth:
      chain:
        middlewares:
          # - middlewares-crowdsec-bouncer # Uncomment when CrowdSec is running
          - middlewares-rate-limit
          - middlewares-https-redirectscheme
          - middlewares-secure-headers

    chain-no-auth-no-redirect:
      chain:
        middlewares:
          # - middlewares-crowdsec-bouncer # Uncomment when CrowdSec is running
          - middlewares-rate-limit
          - middlewares-secure-headers

    chain-no-auth-wp:
      chain:
        middlewares:
          # - middlewares-crowdsec-bouncer # Uncomment when CrowdSec is running
          - middlewares-rate-limit
          - middlewares-https-redirectscheme
          - middlewares-secure-headers-wp
          - middlewares-compress

    chain-no-auth-cors:
      chain:
        middlewares:
          # - middlewares-crowdsec-bouncer # Uncomment when CrowdSec is running
          - middlewares-rate-limit
          - middlewares-https-redirectscheme
          - middlewares-secure-headers-cors

    chain-basic-auth:
      chain:
        middlewares:
          # - middlewares-crowdsec-bouncer # Uncomment when CrowdSec is running
          - middlewares-rate-limit
          - middlewares-https-redirectscheme
          - middlewares-secure-headers
          - middlewares-basic-auth
          - middlewares-compress

    chain-oauth:
      chain:
        middlewares:
          # - middlewares-crowdsec-bouncer # Uncomment when CrowdSec is running
          - middlewares-rate-limit
          - middlewares-https-redirectscheme
          - middlewares-secure-headers
          - middlewares-oauth
          - middlewares-compress

    chain-oauth-wp:
      chain:
        middlewares:
          # - middlewares-crowdsec-bouncer # Uncomment when CrowdSec is running
          - middlewares-rate-limit
          - middlewares-https-redirectscheme
          - middlewares-secure-headers-wp
          - middlewares-oauth
          - middlewares-compress

    chain-authelia:
      chain:
        middlewares:
          # - middlewares-crowdsec-bouncer # Uncomment when CrowdSec is running
          - middlewares-rate-limit
          - middlewares-https-redirectscheme
          - middlewares-secure-headers
          - middlewares-authelia
          - middlewares-compress

    chain-authelia-wp:
      chain:
        middlewares:
          # - middlewares-crowdsec-bouncer # Uncomment when CrowdSec is running
          - middlewares-rate-limit
          - middlewares-https-redirectscheme
          - middlewares-secure-headers-wp
          - middlewares-authelia
          - middlewares-compress
